name: Deploy to cPanel via FTP

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Deploy to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.ftp.hyperphp.com}}
          username: ${{ secrets.hp_35151586}}
          password: ${{ secrets.tucker567}}
          port: 21 # (or the port your host provides)
          protocol: ftp # (or 'sftp' if supported)
          local-dir: ./ # (repo root)
          server-dir: /public_html/ # (your cPanel web root)
